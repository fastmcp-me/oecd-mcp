# Supabase Configuration (Optional - for caching)
# Enable caching by setting these environment variables

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-api-key

# Note: Caching is optional. If not configured, the server will run without cache.
# Cache provides ~40x speedup for repeated queries and reduces OECD API rate limiting.
